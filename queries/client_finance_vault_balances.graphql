{
  vaults(
    orderBy:createdAt
    orderDirection:asc
    first:$first,
    skip:$skip
  ) {
    dao
    createdAt
    
    balances {
      vault{
      id
      }
      id
      token{
        id
        name
        symbol
        decimals
      }
      balance
    }
  }
}